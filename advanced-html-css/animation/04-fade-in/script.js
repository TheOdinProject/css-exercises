// Select the fade-in element
const fadeInBox = document.querySelector(".fadein-container");

// When the page loads, trigger the fade-in effect
window.addEventListener("load", () => {
  fadeInBox.classList.add("visible");
});
